# EMTO BCC 间隙空球 EOS 失败分析

## 1. 问题陈述

在 EMTO-CPA 框架下，所有包含八面体间隙空球（NQ>1）的 BCC 结构计算都无法产生正常的状态方程（EOS）极小值。E(SWS) 曲线呈单调递减（能量随体积减小而持续降低），缺少物理上应有的排斥-吸引平衡点。

这一问题在 Fe-C 和 Nb-C 两个体系、方案 A (NQ=4) 和方案 B (NQ=8)、FCC 和 BCC 两种结构中全部复现，与碳浓度和间隙球半径选择均无关。

---

## 2. 结构构建

### 2.1 BCC 八面体间隙位的晶体学

BCC 常规胞含 2 个金属原子和 6 个等价的八面体间隙位（Wyckoff 6b, Im-3m）。在 BCC 原胞中，体心平移将 6 个位化约为 3 个独立位，故：

- **方案 A (LAT=2, NQ=4)**: BCC 原胞, 1 金属 + 3 八面体
- **方案 B (LAT=1, NQ=8)**: SC 常规胞, 2 金属 + 6 八面体

### 2.2 EMTO 输入构建

结构文件（KSTR/BMDL/SHAPE）使用 pyemto 生成，存于共享目录：

```
lattice/bcc/       NQ=1 纯 BCC（参考基准）
lattice/bcc_oct/   NQ=8 BCC+八面体（方案 B）
lattice/fcc_oct/   NQ=2 FCC+八面体
```

KSTR 关键参数（bcc_oct）：
- LAT=1 (SC), NQ3=8, DMAX=1.4375
- 8 个位点的分数坐标完整列出
- a/w 参数全部设为 0.70（Voronoi 截断比）

KGRN 中的球半径设置：
- 金属位: S(ws) = 1.0（默认）
- 间隙位: S(ws) = 0.77（Niessen 2023 文献值，用于 FCC 间隙）
- 实际半径 R_i = SWS × S(ws)_i

### 2.3 SWS 定义

EMTO 中 SWS 是"平均 Wigner-Seitz 半径"，定义为：

```
V_cell = NQ × (4π/3) × SWS³
```

对 NQ=1 BCC: SWS = (3a³/2/(4π))^(1/3) ≈ 3.07 Bohr (Nb)
对 NQ=8 SC:  SWS = (3a³/(4π×8))^(1/3) ≈ 1.93 Bohr (Nb)

两者通过 SWS₁ = 4^(1/3) × SWS₈ = 1.587 × SWS₈ 换算。

---

## 3. 执行的计算

### 3.1 成功基准：纯 BCC Nb (NQ=1)

- 设置: NM, PBE, 21³ k-mesh, SWS 扫描 3.01–3.13 Bohr (7 点)
- 结果: **正常 EOS 抛物线**, SWS₀ = 3.081 Bohr, B₀ = 163.4 GPa, R² = 0.9987
- 验证: 对应 a₀ ≈ 3.31 Å（实验 3.30 Å），B₀ 接近实验值 170 GPa

### 3.2 间隙空球测试序列

所有 NQ>1 计算使用方案 B (NQ=8, LAT=1)，NM, PBE, 11³ k-mesh：

| 体系 | 间隙占据 | S(ws)_int | SWS 范围 | 等效 SWS₁ 范围 | 收敛 | EOS |
|------|---------|-----------|----------|---------------|------|-----|
| NbVa_bcc | 100% Va | 0.77 | 1.70–1.85 | 2.70–2.94 | 13/14 | 单调↗ |
| NbVa_bcc_eq | 100% Va | 1.00 | 1.87–1.99 | 2.97–3.17 | 4/7 | 单调↗ |
| NbC_bcc | 98% Va + 2% C | 0.77 | 1.87–1.99 | 2.97–3.17 | 7/7 | 单调↗ |
| FeC_bcc | 98% Va + 2% C | 0.77 | 1.62–1.74 | (NQ=4) | 4/7 | KFCD 失败 |
| FeC_fcc | 96% Va + 4% C | 0.77 | 2.04–2.16 | (NQ=2) | 7/7 | 单调↗ |

注："单调↗"指 E(SWS) 随 SWS 增大而增大，即能量随体积压缩而降低。

### 3.3 排除实验设计

```
对比 1: NbVa_bcc vs NbVa_bcc_eq
  变量: S(ws)_int 从 0.77 → 1.0
  结论: 两者均单调 → S(ws) 选择不是根因

对比 2: NbVa_bcc vs NbC_bcc
  变量: 间隙浓度从 0% C → 2% C
  结论: 两者均单调 → 碳原子不是根因

对比 3: Nb_bcc (NQ=1) vs NbVa_bcc_eq (NQ=8, S(ws)=1.0)
  变量: NQ 从 1 → 8（加入空球）
  结论: NQ=1 有极小, NQ=8 无 → 间隙空球结构本身是根因
```

---

## 4. 定量数据分析

### 4.1 能量斜率对比

| 体系 | dE/dSWS | 说明 |
|------|---------|------|
| Nb NQ=1 (平衡附近) | ~0 mRy/Bohr | 抛物线底部，正常 |
| Nb NQ=1 (左壁) | −80 mRy/Bohr | 排斥壁，正常 |
| Nb NQ=1 (右壁) | +50 mRy/Bohr | 膨胀壁，正常 |
| NbVa_bcc_eq (per Nb) | **+1831 mRy/Bohr** | 比 NQ=1 EOS 曲率大 **~600 倍** |
| NbVa_bcc (per Nb) | **+840 mRy/Bohr** | 比 NQ=1 EOS 曲率大 **~300 倍** |

**EOS 的总阱深（NQ=1）仅约 3 mRy**，而间隙空球引入的伪能量斜率高达数百 mRy/Bohr。真实 EOS 曲率完全被掩盖。

### 4.2 绝对能量偏移

在等效体积 SWS_Nb ≈ 3.07 Bohr（Nb 平衡点）：

```
E(NQ=1)           = −7640.951 Ry/Nb
E(NQ=8, eq)/2     = −7650.862 Ry/Nb  [S(ws)=1.0, 完美体积守恒]

幻影能量偏移 ΔE   = −9.91 Ry/Nb = −134.8 eV/Nb
```

这个 ~10 Ry/Nb 的偏移来自 6 个空球的一电子能量、Madelung 势和交换关联贡献。它不是常数——其体积依赖性（斜率 ~1.8 Ry/Bohr）正是问题所在。

### 4.3 空球电荷溢出的体积依赖性

从 KGRN 多极矩（monopole = 球内电子数）提取的数据：

**NbVa_bcc (S(ws) = 0.77)**

| SWS (Bohr) | Q(Nb) | Q(Va) | Q_total | Va 总电子 |
|-------------|-------|-------|---------|-----------|
| 1.700 | 3.61 | 0.35 | 9.34 | 6×0.35 = 2.13 |
| 1.850 | 2.08 | 0.75 | 8.69 | 6×0.75 = 4.53 |

**NbVa_bcc_eq (S(ws) = 1.0)**

| SWS (Bohr) | Q(Nb) | Q(Va) | Q_total | Va 总电子 |
|-------------|-------|-------|---------|-----------|
| 1.935 | 4.88 | 0.45 | 12.45 | 6×0.45 = 2.69 |
| 1.995 | 10.91 | 0.39 | 24.15 | 6×0.39 = 2.33 |

关键观察：

1. **空球不是真正"空"的**：Z=0 的 Va 球捕获了 0.35–0.75 个电子/球（取决于体积和 S(ws)），总共有 2–5 个电子被"溢出"到空球中
2. **电荷分配对体积极其敏感**：SWS 仅变化 9%（1.700→1.850），Va 球电荷翻倍（0.35→0.75）
3. **等球情况下 Nb 球电荷异常膨胀**：S(ws)=1.0 时，SWS 从 1.935 增到 1.995（仅 3%），Nb 球内电子从 4.88 暴增到 10.91——这在物理上是不可能的，说明 EMTO 的电荷分区方案在此场景下严重失控

### 4.4 ASA 体积守恒检查

ASA 要求所有球体积之和等于胞体积：Σ S(ws)_i³ = NQ

| 配置 | Σ S(ws)³ | 需要 | 亏缺 |
|------|---------|------|------|
| NQ=8, S(ws)_int=0.77, S(ws)_Nb=1.0 | 4.74 | 8 | **40.8%** |
| NQ=8, S(ws)_int=1.0, S(ws)_Nb=1.0 | 8.00 | 8 | 0% |
| NQ=4, S(ws)_int=0.77, S(ws)_Fe=1.0 | 2.37 | 4 | **40.8%** |

S(ws)=0.77 时有 41% 的胞体积不被任何球覆盖——严重违反 ASA 假设。但即使 S(ws)=1.0（完美守恒）也不能修复 EOS 问题。

---

## 5. 根因分析

### 5.1 近因：空球电荷溢出引入的伪压力

EMTO 总能量可以分解为：

```
E_total(NQ=8) = E_band + E_Madelung + E_xc + E_kinetic
              = Σ_i [ε_i(site) + V_Mad(site) + E_xc(site) + ...]
```

6 个 Z=0 空球虽然没有原子核，但在 SCF 循环中从相邻金属原子获得溢出电荷（0.3–0.8 e⁻/球）。这些电荷贡献了：

1. **一电子带能 (EONE)**：空球的电子态在能带中形成弥散态，其本征值随体积变化
2. **Madelung 能**：带电空球改变了整个晶格的静电势分布。空球的有效电荷 Q_Va ≈ −0.4e 与邻近 Nb 的 Q_Nb ≈ +0.4e 形成偶极矩，其 Madelung 能随球间距（∝ a ∝ SWS）强烈变化
3. **交换关联能 (E_xc)**：空球内的低密度溢出电荷贡献额外的 xc 能

这三项的体积导数（∂E_phantom/∂V）构成一个"伪压力"，其量级（~1800 mRy/Bohr）远超真实 EOS 的曲率（~50 mRy/Bohr），彻底掩盖了平衡点。

### 5.2 远因：EMTO ASA 在开放间隙结构中的内在局限

BCC 八面体间隙洞远小于 FCC。关键几何对比：

| 参数 | FCC 八面体 | BCC 八面体 |
|------|-----------|-----------|
| 最近邻金属距离 | a/√2 = 2.03 Å (6 个等距) | a/2 = 1.43 Å (2 近) + a√2/2 = 2.03 Å (4 远) |
| 最大可容纳球半径 | 0.52 Å | 0.36 Å |
| 点群对称性 | O_h (立方) | D_4h (四方, 各向异性) |
| 金属:间隙球数比 | 1:1 (FCC NQ=2) | 1:3 (BCC NQ=4) 或 2:6 (NQ=8) |

BCC 的特殊困难：

- **间隙/金属球数比高**: 3:1（BCC）vs 1:1（FCC），空球贡献的伪能量在总能中占比更大
- **八面体洞各向异性 (D_4h)**: 2 个近邻（a/2）和 4 个远邻（a√2/2）导致 Voronoi 多面体形状极不规则，ASA 的球形近似误差更大
- **势球严重重叠**: 即使 S(ws)_int=0.77，金属-间隙球重叠达 31%；而 BCC 最近邻距离仅 a/2 使得重叠集中在极短距离上

### 5.3 为什么 S(ws) 调整无法修复

S(ws)=0.77 和 S(ws)=1.0 都失败的原因是双向困境：

- **S(ws)=0.77**（缩球）：势球仅覆盖 59% 胞体积 → 大量间隙区域的电子密度被遗漏 → Madelung 和 xc 能出现系统误差 → 伪能量斜率
- **S(ws)=1.0**（等球）：体积守恒满足，但空球过大 → 金属原子的尾部电子被过度"分配"到空球 → 电荷分区失控（Nb 球内电荷从 4.88 暴涨到 10.91）→ 伪能量斜率

两种极端的失败机制不同，但效果一样：体积依赖的伪能量主导了 E(V) 关系。

---

## 6. 可能的解决方案

### 6.1 方案 A：体积守恒 + 优化 S(ws) 比

当前参数直接采用了 Niessen 2023 的 FCC 值 (0.77)，未针对 BCC 做优化。正确做法：

1. **强制体积守恒**: 设 S(ws)_Va = α, 则 S(ws)_Nb = ((NQ - N_Va × α³) / N_Nb)^(1/3)
   - 对 NQ=4: S(ws)_Fe = ((4 − 3α³)/1)^(1/3)
   - 对 NQ=8: S(ws)_Nb = ((8 − 6α³)/2)^(1/3)
2. **扫描 α**: 在 0.50–0.90 范围扫描，找使 EOS 曲率最合理（或总能最低）的 α
3. **验证 E_NQ4(C=0%) ≈ E_NQ1**: 纯 Va 间隙的 NQ=4 能量应在几 mRy/atom 内接近 NQ=1 基准

但鉴于 S(ws)=1.0（完美守恒）已经失败，此方案可能不够。

### 6.2 方案 B：改用 NQ=4 方案 A (LAT=2)

方案 A 的间隙/金属球数比为 3:1（vs 方案 B 的 6:2 = 3:1），比率相同，但：

- 原胞更小 → k 网格效率更高 → 可用更密 k 点
- KSTR/BMDL 中的 Voronoi 多面体数更少 → 截断误差可能更小
- 当前 FeC_bcc (NQ=4) 因 MPI 问题未产出有效能量，**应优先修复并重新运行**

修复步骤：
```bash
# 方法1: 用 srun 启动（推荐）
srun -n 4 kgrn_cpa < input.kgrn > output 2>&1
# 方法2: 设 NCPU=001 串行运行
# 在 .kgrn 文件中改 NCPU=001
```

### 6.3 方案 C：检查 EMTO 文献中的成功案例

已知的 EMTO 间隙计算成功案例：

- **Niessen et al. (2023)**: FCC Fe-C, NQ=2, S(ws)_C = 0.77 — 报告了合理的固溶能
- **Punkkinen et al. (PRB 81, 045122, 2010)**: 间隙计算中使用 Lmaxt=4 (spdfg 基组)
- **EMTO book (Vitos, 2007)**: 空球处理的理论基础

需要仔细对比文献中的以下参数，看我们是否遗漏了什么：
- KSTR 中的 DMAX 值（我们用 1.4375，纯 BCC 用 2.1992，差异显著）
- BMDL 中的 Madelung 截断参数
- FCD 步骤中的 kappa 值选择（kappaw 参数）
- 是否需要 SOFC=Y（soft-core 近似）以外的设置

### 6.4 方案 D：SWS 扫描范围可能不对

重新检视数据：NbVa_bcc_eq (S(ws)=1.0) 的扫描范围 SWS₈=1.87–1.99 对应 a = 3.15–3.40 Å。Nb 平衡 a = 3.30 Å 在此范围内。但只有 4/7 点收敛，且恰好是**偏大体积**的 4 个点（SWS₈=1.935–1.995），偏小体积的 3 个点全部发散。

在 NQ=8 中，平衡 SWS₈ ≈ 1.935 对应的是 SWS₁ = 3.07（与 NQ=1 平衡一致）。但此点处 E(NQ=8) 的斜率仍为 +1831 mRy/Bohr（强排斥），意味着 NQ=8 的"伪平衡点"可能在更小的 SWS 处——但那些点已经发散。

→ 建议：尝试在 SWS₈ = 1.50–1.90 范围重新扫描 NbVa_bcc_eq，覆盖可能被伪压力移到的"伪平衡"体积。

### 6.5 方案 E：完全绕过间隙空球

如果 ASA 在 BCC 间隙问题上本质不可行，可考虑：

1. **超胞方法**: 用 2×2×2 或 3×3×3 BCC 超胞（16 或 54 个金属原子），将 1 个金属原子替换为空位+碳的复合体，不引入独立的间隙子格。但这丧失了 CPA 的无序处理优势
2. **FCC 间隙作为基准**: FCC 八面体间隙的 ASA 问题较轻（球数比 1:1、O_h 对称性），可先在 FCC 上标定方法，再外推到 BCC
3. **切换到 EMTO-FPLMTO**: 全势版本不依赖 ASA，但计算量大幅增加

---

## 7. 下一步建议

**优先级排序：**

1. **修复 FeC_bcc NQ=4 的 MPI 问题**，获得方案 A 的能量数据，看 NQ=4 是否比 NQ=8 更好（空球数从 6 减到 3）
2. **跑 NQ=4 的 C=0% 纯 Va 版本**（对标 NQ=1 纯 BCC），量化方案 A 的伪能量大小
3. **做体积守恒的 S(ws) 扫描**：在 NQ=4 方案 A 下，用 α = 0.50–0.90 扫描，每个 α 做 7 点 EOS，找可能的最优 S(ws)
4. **仔细对比 Niessen 2023 的 FCC 参数**，特别是 DMAX、kappaw、KSTR 截断等"暗参数"
5. 如果以上都不行，考虑**方案 E**

---

## 附录：关键文件索引

| 文件 | 用途 |
|------|------|
| `lattice/bcc_oct/*.kstr` | NQ=8 结构的 KSTR 输入 |
| `NbVa_bcc_eos.py` | Nb + 100% Va 的输入生成脚本 |
| `NbVa_bcc_eq_eos.py` | Nb + 100% Va (等球) 输入生成脚本 |
| `NbVa_bcc/kgrn/*.prn` | KGRN 输出（含多极矩数据） |
| `NbVa_bcc/kfcd/*.prn` | KFCD 总能量输出 |
| `Nb_bcc/fit/Nb_bcc_eos.png` | NQ=1 参考 EOS 图 |
| `NbVa_bcc/fit/NbVa_bcc_eos.png` | NQ=8 单调能量图 |
| `EMTO-CPA_BCC_Interstitial_Construction_Guide.md` | 完整建模指南 |
